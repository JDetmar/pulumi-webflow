name: webflow-webhook-example-python
runtime: python
description: Example of creating and managing Webhook resources with Pulumi and Webflow

config:
  siteId:
    description: The Webflow site ID
    secret: true
  environment:
    description: Deployment environment (development, staging, production)
    default: development
