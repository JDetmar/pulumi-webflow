name: webflow-collectionfield-example
runtime: nodejs
description: Example of creating and managing CollectionField resources with Pulumi and Webflow

config:
  collectionId:
    description: The Webflow collection ID (24-character hex string)
    secret: true
  environment:
    description: Deployment environment (development, staging, production)
    default: development
