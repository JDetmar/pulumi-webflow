{
  "name": "webflow",
  "displayName": "Webflow (Unofficial)",
  "description": "Unofficial community-maintained Pulumi provider for managing Webflow sites, redirects, and robots.txt. Not affiliated with Pulumi Corporation or Webflow, Inc.",
  "homepage": "https://github.com/jdetmar/pulumi-webflow",
  "namespace": "webflow",
  "language": {
    "csharp": {
      "rootNamespace": "Pulumi"
    },
    "go": {
      "importBasePath": "github.com/jdetmar/pulumi-webflow/sdk/go/webflow"
    },
    "java": {
      "basePackage": "io.github.jdetmar.pulumi",
      "buildFiles": "gradle"
    },
    "nodejs": {
      "packageDescription": "Unofficial community-maintained Pulumi provider for Webflow. Not affiliated with Pulumi Corporation or Webflow, Inc.",
      "packageName": "@jdetmar/pulumi-webflow"
    }
  },
  "config": {
    "variables": {
      "apiToken": {
        "type": "string",
        "description": "Webflow API v2 bearer token for authentication. Can also be set via WEBFLOW_API_TOKEN environment variable.",
        "secret": true
      }
    }
  },
  "types": {
    "webflow:index:CustomScriptArgs": {
      "properties": {
        "attributes": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Optional developer-specified key/value pairs applied as HTML attributes to the script tag. Example: {'data-config': 'my-value'}. These attributes are passed directly to the script tag."
        },
        "id": {
          "type": "string",
          "description": "The unique identifier of the registered custom code script. The script must first be registered to the site using the RegisterScript resource. Examples: 'cms_slider', 'analytics', 'custom_widget'"
        },
        "location": {
          "type": "string",
          "description": "The location where the script is placed on the page. Valid values: 'header' (placed in the <head> section), 'footer' (placed before </body>). Scripts in the header execute before page content loads, while footer scripts execute after the page has loaded."
        },
        "version": {
          "type": "string",
          "description": "The semantic version string for the registered script (e.g., '1.0.0', '0.1.2'). This version must exist for the registered script ID. When you update the version, a different version of the script will be applied."
        }
      },
      "type": "object",
      "required": [
        "id",
        "version",
        "location"
      ]
    },
    "webflow:index:GetTokenInfoApplication": {
      "properties": {
        "description": {
          "type": "string",
          "description": "The application description."
        },
        "displayName": {
          "type": "string",
          "description": "The human-readable name of the application."
        },
        "homepage": {
          "type": "string",
          "description": "The application homepage URL."
        },
        "id": {
          "type": "string",
          "description": "The unique identifier for the application."
        }
      },
      "type": "object",
      "required": [
        "id",
        "description",
        "homepage",
        "displayName"
      ]
    },
    "webflow:index:GetTokenInfoAuthorization": {
      "properties": {
        "authorizedTo": {
          "$ref": "#/types/webflow:index:GetTokenInfoAuthorizedTo",
          "description": "The resources this token is authorized to access."
        },
        "createdOn": {
          "type": "string",
          "description": "The timestamp when this authorization was created (RFC3339 format)."
        },
        "grantType": {
          "type": "string",
          "description": "The OAuth grant type used to obtain this token (e.g., 'authorization_code')."
        },
        "id": {
          "type": "string",
          "description": "The unique identifier for this authorization."
        },
        "lastUsed": {
          "type": "string",
          "description": "The timestamp when this token was last used (RFC3339 format)."
        },
        "rateLimit": {
          "type": "integer",
          "description": "The rate limit for this token in requests per minute."
        },
        "scope": {
          "type": "string",
          "description": "The OAuth scopes granted to this token (space or comma separated)."
        }
      },
      "type": "object",
      "required": [
        "id",
        "createdOn",
        "lastUsed",
        "grantType",
        "rateLimit",
        "scope",
        "authorizedTo"
      ]
    },
    "webflow:index:GetTokenInfoAuthorizedTo": {
      "properties": {
        "siteIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of site IDs this token is authorized to access."
        },
        "userIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of user IDs this token is authorized to access."
        },
        "workspaceIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of workspace IDs this token is authorized to access."
        }
      },
      "type": "object",
      "required": [
        "siteIds",
        "workspaceIds",
        "userIds"
      ]
    },
    "webflow:index:NodeContentUpdate": {
      "properties": {
        "nodeId": {
          "type": "string",
          "description": "The unique identifier for the DOM node to update. This ID comes from the page's DOM structure and must exist on the page. Retrieve node IDs using GET /pages/{page_id}/dom endpoint."
        },
        "text": {
          "type": "string",
          "description": "The new text content for the node. This will replace the existing text content in the specified node. Only applicable to text nodes or elements containing text."
        }
      },
      "type": "object",
      "required": [
        "nodeId",
        "text"
      ]
    },
    "webflow:index:PageCustomCodeScript": {
      "properties": {
        "attributes": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Optional developer-specified key/value pairs for script attributes. These attributes can be used by the script to customize its behavior on this page."
        },
        "id": {
          "type": "string",
          "description": "The unique identifier of a registered custom code script. This must be a script that was previously registered using the RegisteredScript resource. Script IDs are assigned by Webflow when the script is registered."
        },
        "location": {
          "type": "string",
          "description": "Where the script should be applied on the page. Must be either 'header' (loaded in page header) or 'footer' (loaded at end of page). Use 'header' for scripts that don't depend on DOM elements. Use 'footer' for scripts that need to run after DOM is fully loaded."
        },
        "version": {
          "type": "string",
          "description": "The semantic version string for the registered script (e.g., '1.0.0'). This version must match a registered version of the script. You can have multiple versions of the same script registered."
        }
      },
      "type": "object",
      "required": [
        "id",
        "version",
        "location",
        "attributes"
      ]
    },
    "webflow:index:PageInfo": {
      "properties": {
        "archived": {
          "type": "boolean"
        },
        "collectionId": {
          "type": "string"
        },
        "createdOn": {
          "type": "string"
        },
        "draft": {
          "type": "boolean"
        },
        "lastUpdated": {
          "type": "string"
        },
        "pageId": {
          "type": "string"
        },
        "parentId": {
          "type": "string"
        },
        "siteId": {
          "type": "string"
        },
        "slug": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "type": "object",
      "required": [
        "pageId",
        "siteId",
        "title",
        "slug",
        "createdOn",
        "lastUpdated",
        "archived",
        "draft"
      ]
    }
  },
  "provider": {
    "properties": {
      "apiToken": {
        "type": "string",
        "description": "Webflow API v2 bearer token for authentication. Can also be set via WEBFLOW_API_TOKEN environment variable.",
        "secret": true
      }
    },
    "inputProperties": {
      "apiToken": {
        "type": "string",
        "description": "Webflow API v2 bearer token for authentication. Can also be set via WEBFLOW_API_TOKEN environment variable.",
        "secret": true
      }
    }
  },
  "resources": {
    "webflow:index:Asset": {
      "description": "Manages assets (images, files, documents) for a Webflow site. This resource allows you to upload and manage files that can be used in your Webflow site. Note: Assets are immutable - changing any property will delete and recreate the asset.",
      "properties": {
        "assetId": {
          "type": "string",
          "description": "The Webflow-assigned asset ID (read-only). This unique identifier can be used to reference the asset in API calls."
        },
        "assetUrl": {
          "type": "string",
          "description": "The direct S3 URL for the asset (read-only). This is the raw S3 location where the file is stored."
        },
        "contentType": {
          "type": "string",
          "description": "The MIME type of the asset (read-only). Examples: 'image/png', 'image/jpeg', 'application/pdf'. Determined by the fileName extension."
        },
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the asset metadata was created (RFC3339 format, read-only). This is set when the asset is registered with Webflow."
        },
        "fileHash": {
          "type": "string",
          "description": "MD5 hash of the file content (required). Webflow uses this hash to identify and deduplicate assets. Generate using: md5sum <filename> (Linux) or md5 <filename> (macOS). Example: 'd41d8cd98f00b204e9800998ecf8427e'."
        },
        "fileName": {
          "type": "string",
          "description": "The name of the file to upload, including the extension. Examples: 'logo.png', 'hero-image.jpg', 'document.pdf'. The file name must not exceed 255 characters and should not contain invalid characters (<, >, :, \", |, ?, *)."
        },
        "fileSource": {
          "type": "string",
          "description": "The source of the file to upload. For the current implementation, this is a reference field. In future versions, this may support URLs or local file paths for automatic upload. Examples: 'https://example.com/logo.png', '/path/to/local/file.png'."
        },
        "hostedUrl": {
          "type": "string",
          "description": "The Webflow CDN URL where the asset will be hosted (read-only). This URL becomes accessible after completing the S3 upload. Example: 'https://assets.website-files.com/.../logo.png'."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the asset was last modified (RFC3339 format, read-only). For most assets, this will be the same as createdOn since assets are immutable."
        },
        "parentFolder": {
          "type": "string",
          "description": "Optional folder ID where the asset will be organized in the Webflow Assets panel. If not specified, the asset will be placed at the root level. Example: '5f0c8c9e1c9d440000e8d8c4'."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "size": {
          "type": "integer",
          "description": "The size of the asset in bytes (read-only). This is the actual size of the uploaded file."
        },
        "uploadDetails": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "AWS S3 POST form fields required to complete the upload (read-only). Include these as form fields when POSTing the file to uploadUrl. Keys: acl, bucket, key, Content-Type, X-Amz-Algorithm, X-Amz-Credential, X-Amz-Date, Policy, X-Amz-Signature, success_action_status, Cache-Control."
        },
        "uploadUrl": {
          "type": "string",
          "description": "The presigned S3 URL for uploading the file content (read-only). Use this URL along with uploadDetails to complete the asset upload. See AWS S3 POST documentation: https://docs.aws.amazon.com/AmazonS3/latest/API/RESTObjectPOST.html"
        }
      },
      "required": [
        "siteId",
        "fileName",
        "fileHash"
      ],
      "inputProperties": {
        "fileHash": {
          "type": "string",
          "description": "MD5 hash of the file content (required). Webflow uses this hash to identify and deduplicate assets. Generate using: md5sum <filename> (Linux) or md5 <filename> (macOS). Example: 'd41d8cd98f00b204e9800998ecf8427e'."
        },
        "fileName": {
          "type": "string",
          "description": "The name of the file to upload, including the extension. Examples: 'logo.png', 'hero-image.jpg', 'document.pdf'. The file name must not exceed 255 characters and should not contain invalid characters (<, >, :, \", |, ?, *)."
        },
        "fileSource": {
          "type": "string",
          "description": "The source of the file to upload. For the current implementation, this is a reference field. In future versions, this may support URLs or local file paths for automatic upload. Examples: 'https://example.com/logo.png', '/path/to/local/file.png'."
        },
        "parentFolder": {
          "type": "string",
          "description": "Optional folder ID where the asset will be organized in the Webflow Assets panel. If not specified, the asset will be placed at the root level. Example: '5f0c8c9e1c9d440000e8d8c4'."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        }
      },
      "requiredInputs": [
        "siteId",
        "fileName",
        "fileHash"
      ]
    },
    "webflow:index:AssetFolder": {
      "description": "Manages asset folders for organizing files in a Webflow site. This resource allows you to create folders to organize your assets (images, documents, etc.) in the Webflow Assets panel. NOTE: The Webflow API does not support deleting or updating asset folders. Deleting this resource will only remove it from Pulumi state, not from Webflow. Any changes to folder properties will require creating a new folder.",
      "properties": {
        "assets": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of asset IDs currently contained in this folder (read-only). This is automatically populated by Webflow when assets are added to the folder."
        },
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the folder was created (RFC3339 format, read-only). This is automatically set when the folder is created."
        },
        "displayName": {
          "type": "string",
          "description": "The human-readable name for the asset folder. This name appears in the Webflow Assets panel and helps organize your files. Examples: 'Images', 'Documents', 'Icons', 'Hero Backgrounds'. Maximum length: 255 characters."
        },
        "folderId": {
          "type": "string",
          "description": "The Webflow-assigned folder ID (read-only). This unique identifier can be used to reference the folder in other resources, such as when uploading assets to this folder."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the folder was last modified (RFC3339 format, read-only). This is updated when assets are added or removed from the folder."
        },
        "parentFolder": {
          "type": "string",
          "description": "Optional ID of the parent folder for creating nested folder structures. If not specified, the folder will be created at the root level of the Assets panel. Example: '5f0c8c9e1c9d440000e8d8c4'."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        }
      },
      "required": [
        "siteId",
        "displayName"
      ],
      "inputProperties": {
        "displayName": {
          "type": "string",
          "description": "The human-readable name for the asset folder. This name appears in the Webflow Assets panel and helps organize your files. Examples: 'Images', 'Documents', 'Icons', 'Hero Backgrounds'. Maximum length: 255 characters."
        },
        "parentFolder": {
          "type": "string",
          "description": "Optional ID of the parent folder for creating nested folder structures. If not specified, the folder will be created at the root level of the Assets panel. Example: '5f0c8c9e1c9d440000e8d8c4'."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        }
      },
      "requiredInputs": [
        "siteId",
        "displayName"
      ]
    },
    "webflow:index:Collection": {
      "description": "Manages CMS collections for a Webflow site. Collections are containers for structured content items (blog posts, products, etc.). Note: Webflow collections do not support updates - any changes require replacement (delete + recreate).",
      "properties": {
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the collection was created (RFC3339 format). This is automatically set by Webflow and is read-only."
        },
        "displayName": {
          "type": "string",
          "description": "The human-readable name of the collection (e.g., 'Blog Posts', 'Products', 'Team Members'). This name appears in the Webflow CMS interface. Maximum length: 255 characters."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the collection was last updated (RFC3339 format). This is automatically updated by Webflow and is read-only."
        },
        "singularName": {
          "type": "string",
          "description": "The singular form of the collection name (e.g., 'Blog Post' for 'Blog Posts', 'Product' for 'Products'). Used in the CMS UI when referring to individual items. Maximum length: 255 characters."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "slug": {
          "type": "string",
          "description": "The URL-friendly slug for the collection (optional, e.g., 'blog-posts', 'products'). If not provided, Webflow will auto-generate a slug from the displayName. The slug determines the URL path for collection items."
        }
      },
      "required": [
        "siteId",
        "displayName",
        "singularName"
      ],
      "inputProperties": {
        "displayName": {
          "type": "string",
          "description": "The human-readable name of the collection (e.g., 'Blog Posts', 'Products', 'Team Members'). This name appears in the Webflow CMS interface. Maximum length: 255 characters."
        },
        "singularName": {
          "type": "string",
          "description": "The singular form of the collection name (e.g., 'Blog Post' for 'Blog Posts', 'Product' for 'Products'). Used in the CMS UI when referring to individual items. Maximum length: 255 characters."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "slug": {
          "type": "string",
          "description": "The URL-friendly slug for the collection (optional, e.g., 'blog-posts', 'products'). If not provided, Webflow will auto-generate a slug from the displayName. The slug determines the URL path for collection items."
        }
      },
      "requiredInputs": [
        "siteId",
        "displayName",
        "singularName"
      ]
    },
    "webflow:index:CollectionField": {
      "description": "Manages fields for a Webflow CMS collection. Collection fields define the structure of content items in a collection. Note: The field type cannot be changed after creation - changing it requires replacement (delete + recreate).",
      "properties": {
        "collectionId": {
          "type": "string",
          "description": "The Webflow collection ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find collection IDs via the Webflow API or dashboard. This field will be validated before making any API calls."
        },
        "displayName": {
          "type": "string",
          "description": "The human-readable name of the field (e.g., 'Title', 'Description', 'Author'). This name appears in the Webflow CMS interface. Maximum length: 255 characters."
        },
        "fieldId": {
          "type": "string",
          "description": "The Webflow-assigned field ID (read-only). This ID is automatically generated when the field is created."
        },
        "helpText": {
          "type": "string",
          "description": "Optional help text shown in the CMS interface (e.g., 'Enter the article title'). Helps content editors understand what to enter in this field."
        },
        "isEditable": {
          "type": "boolean",
          "description": "Whether the field can be edited (read-only). System fields may not be editable."
        },
        "isRequired": {
          "type": "boolean",
          "description": "Whether the field is required (optional, defaults to false). When true, content items must provide a value for this field."
        },
        "slug": {
          "type": "string",
          "description": "The URL-friendly slug for the field (optional, e.g., 'title', 'description'). If not provided, Webflow will auto-generate a slug from the displayName. The slug is used in API requests and exports."
        },
        "type": {
          "type": "string",
          "description": "The field type (e.g., 'PlainText', 'RichText', 'Image', 'Number'). Supported types: PlainText, RichText, Image, MultiImage, Video, Link, Email, Phone, Number, DateTime, Switch, Color, Option, File, Reference, MultiReference. IMPORTANT: Cannot be changed after creation - changing this requires replacement."
        },
        "validations": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Type-specific validation rules (optional). Different field types support different validations. Example for Number type: {\"min\": 0, \"max\": 100}. Example for PlainText type: {\"maxLength\": 500}. Refer to Webflow API documentation for validation options for each field type."
        }
      },
      "required": [
        "collectionId",
        "type",
        "displayName"
      ],
      "inputProperties": {
        "collectionId": {
          "type": "string",
          "description": "The Webflow collection ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find collection IDs via the Webflow API or dashboard. This field will be validated before making any API calls."
        },
        "displayName": {
          "type": "string",
          "description": "The human-readable name of the field (e.g., 'Title', 'Description', 'Author'). This name appears in the Webflow CMS interface. Maximum length: 255 characters."
        },
        "helpText": {
          "type": "string",
          "description": "Optional help text shown in the CMS interface (e.g., 'Enter the article title'). Helps content editors understand what to enter in this field."
        },
        "isRequired": {
          "type": "boolean",
          "description": "Whether the field is required (optional, defaults to false). When true, content items must provide a value for this field."
        },
        "slug": {
          "type": "string",
          "description": "The URL-friendly slug for the field (optional, e.g., 'title', 'description'). If not provided, Webflow will auto-generate a slug from the displayName. The slug is used in API requests and exports."
        },
        "type": {
          "type": "string",
          "description": "The field type (e.g., 'PlainText', 'RichText', 'Image', 'Number'). Supported types: PlainText, RichText, Image, MultiImage, Video, Link, Email, Phone, Number, DateTime, Switch, Color, Option, File, Reference, MultiReference. IMPORTANT: Cannot be changed after creation - changing this requires replacement."
        },
        "validations": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Type-specific validation rules (optional). Different field types support different validations. Example for Number type: {\"min\": 0, \"max\": 100}. Example for PlainText type: {\"maxLength\": 500}. Refer to Webflow API documentation for validation options for each field type."
        }
      },
      "requiredInputs": [
        "collectionId",
        "type",
        "displayName"
      ]
    },
    "webflow:index:CollectionItem": {
      "description": "Manages CMS collection items for a Webflow collection. Collection items represent individual content entries (blog posts, products, etc.) within a CMS collection. Each item has dynamic field data based on the collection schema.",
      "properties": {
        "cmsLocaleId": {
          "type": "string",
          "description": "The locale ID for localized sites (optional, e.g., 'en-US'). Only required if your site uses Webflow's localization features. Leave empty for non-localized sites."
        },
        "collectionId": {
          "type": "string",
          "description": "The Webflow collection ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find collection IDs via the Webflow API or dashboard. This field will be validated before making any API calls."
        },
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the item was created (RFC3339 format, read-only). This is automatically set by Webflow and is immutable."
        },
        "fieldData": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "A map of field slugs to values for the collection item. The field slugs must match the fields defined in the collection schema. Common fields include 'name' (required), 'slug' (required, URL-friendly), and any custom fields you've added to the collection. Example: {\"name\": \"My Blog Post\", \"slug\": \"my-blog-post\", \"content\": \"Post content...\"}"
        },
        "isArchived": {
          "type": "boolean",
          "description": "Whether the item is archived (optional, defaults to false). Archived items are not visible on the published site but remain in the CMS."
        },
        "isDraft": {
          "type": "boolean",
          "description": "Whether the item is a draft (optional, defaults to true). Draft items are not published to the live site. Set to false to publish the item immediately upon creation."
        },
        "itemId": {
          "type": "string",
          "description": "The Webflow-assigned item ID (read-only). This is automatically set by Webflow when the item is created."
        },
        "lastPublished": {
          "type": "string",
          "description": "The timestamp when the item was last published (RFC3339 format, read-only). This is automatically updated by Webflow when the item is published."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the item was last updated (RFC3339 format, read-only). This is automatically updated by Webflow whenever the item is modified."
        }
      },
      "required": [
        "collectionId",
        "fieldData"
      ],
      "inputProperties": {
        "cmsLocaleId": {
          "type": "string",
          "description": "The locale ID for localized sites (optional, e.g., 'en-US'). Only required if your site uses Webflow's localization features. Leave empty for non-localized sites."
        },
        "collectionId": {
          "type": "string",
          "description": "The Webflow collection ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find collection IDs via the Webflow API or dashboard. This field will be validated before making any API calls."
        },
        "fieldData": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "A map of field slugs to values for the collection item. The field slugs must match the fields defined in the collection schema. Common fields include 'name' (required), 'slug' (required, URL-friendly), and any custom fields you've added to the collection. Example: {\"name\": \"My Blog Post\", \"slug\": \"my-blog-post\", \"content\": \"Post content...\"}"
        },
        "isArchived": {
          "type": "boolean",
          "description": "Whether the item is archived (optional, defaults to false). Archived items are not visible on the published site but remain in the CMS."
        },
        "isDraft": {
          "type": "boolean",
          "description": "Whether the item is a draft (optional, defaults to true). Draft items are not published to the live site. Set to false to publish the item immediately upon creation."
        }
      },
      "requiredInputs": [
        "collectionId",
        "fieldData"
      ]
    },
    "webflow:index:EcommerceSettings": {
      "description": "Manages (imports) ecommerce settings for a Webflow site. This is a read-only resource that allows you to track and reference existing ecommerce settings. Ecommerce must be enabled through the Webflow dashboard before this resource can be used. Use this resource to access the site's default currency and verify ecommerce is enabled.",
      "properties": {
        "createdOn": {
          "type": "string",
          "description": "The timestamp when ecommerce was enabled on the site (ISO 8601 format). This is automatically set when ecommerce is enabled and is read-only."
        },
        "defaultCurrency": {
          "type": "string",
          "description": "The three-letter ISO 4217 currency code for the site (e.g., 'USD', 'EUR', 'GBP'). This is the default currency used for ecommerce transactions on this site. This value is set in the Webflow dashboard and is read-only."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). The site must have ecommerce enabled through the Webflow dashboard. You can find your site ID in the Webflow dashboard under Site Settings."
        }
      },
      "required": [
        "siteId",
        "defaultCurrency"
      ],
      "inputProperties": {
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). The site must have ecommerce enabled through the Webflow dashboard. You can find your site ID in the Webflow dashboard under Site Settings."
        }
      },
      "requiredInputs": [
        "siteId"
      ]
    },
    "webflow:index:PageContent": {
      "description": "Manages static content (text) for a Webflow page. This resource allows you to update text content within existing DOM nodes on a page. It does NOT manage page structure or layout - only content within existing nodes. To find node IDs, you must first retrieve the page DOM structure using the Webflow API. \n\n**IMPORTANT LIMITATION:** This resource does NOT support drift detection for content changes. If content is modified outside of Pulumi (via Webflow UI or API), those changes will NOT be detected during 'pulumi refresh' or 'pulumi up'. The resource only verifies that the page still exists. This is due to the complexity of extracting and comparing specific node text from the full DOM structure.",
      "properties": {
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the page content was last updated (RFC3339 format). This is automatically set when content is updated and is read-only."
        },
        "nodes": {
          "type": "array",
          "items": {
            "$ref": "#/types/webflow:index:NodeContentUpdate"
          },
          "description": "List of node content updates to apply. Each update specifies the nodeId (from the page's DOM structure) and the new text content. Node IDs can be retrieved by fetching the page DOM using GET /pages/{page_id}/dom. Only text content in existing nodes can be updated via this resource."
        },
        "pageId": {
          "type": "string",
          "description": "The Webflow page ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c4'). You can find page IDs using the Pages API list endpoint or in the Webflow designer. This field will be validated before making any API calls."
        }
      },
      "required": [
        "pageId",
        "nodes"
      ],
      "inputProperties": {
        "nodes": {
          "type": "array",
          "items": {
            "$ref": "#/types/webflow:index:NodeContentUpdate"
          },
          "description": "List of node content updates to apply. Each update specifies the nodeId (from the page's DOM structure) and the new text content. Node IDs can be retrieved by fetching the page DOM using GET /pages/{page_id}/dom. Only text content in existing nodes can be updated via this resource."
        },
        "pageId": {
          "type": "string",
          "description": "The Webflow page ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c4'). You can find page IDs using the Pages API list endpoint or in the Webflow designer. This field will be validated before making any API calls."
        }
      },
      "requiredInputs": [
        "pageId",
        "nodes"
      ]
    },
    "webflow:index:PageCustomCode": {
      "description": "Manages custom code (JavaScript) scripts applied to a Webflow page. This resource allows you to apply registered custom code scripts to specific pages. Scripts must first be registered using the RegisteredScript resource before they can be applied. All scripts in the configuration will be applied to the page; scripts not listed will be removed.",
      "properties": {
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the page custom code was first created (RFC3339 format). This is automatically set when the configuration is first created and is read-only."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the page custom code was last updated (RFC3339 format). This is automatically set when the configuration is updated and is read-only."
        },
        "pageId": {
          "type": "string",
          "description": "The Webflow page ID (24-character hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c4'). You can find page IDs using the Webflow Pages API or in the Webflow designer. This field will be validated before making any API calls."
        },
        "scripts": {
          "type": "array",
          "items": {
            "$ref": "#/types/webflow:index:PageCustomCodeScript"
          },
          "description": "List of custom code scripts to apply to the page. Each script must reference a script ID and version that have been previously registered. All scripts in this list will be applied to the page; any scripts not listed will be removed from the page."
        }
      },
      "required": [
        "pageId",
        "scripts"
      ],
      "inputProperties": {
        "pageId": {
          "type": "string",
          "description": "The Webflow page ID (24-character hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c4'). You can find page IDs using the Webflow Pages API or in the Webflow designer. This field will be validated before making any API calls."
        },
        "scripts": {
          "type": "array",
          "items": {
            "$ref": "#/types/webflow:index:PageCustomCodeScript"
          },
          "description": "List of custom code scripts to apply to the page. Each script must reference a script ID and version that have been previously registered. All scripts in this list will be applied to the page; any scripts not listed will be removed from the page."
        }
      },
      "requiredInputs": [
        "pageId",
        "scripts"
      ]
    },
    "webflow:index:PageData": {
      "description": "Reads page information from a Webflow site. Pages cannot be created via the API - they must be created in the Webflow designer. Use this data source to retrieve page metadata for use in your infrastructure code. Specify pageId to get a single page, or omit it to list all pages in the site.",
      "properties": {
        "archived": {
          "type": "boolean",
          "description": "Indicates if the page is archived (read-only). Only populated when pageId input is specified."
        },
        "collectionId": {
          "type": "string",
          "description": "The ID of the CMS collection (read-only, optional). Only present for collection pages. Only populated when pageId input is specified."
        },
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the page was created (read-only, RFC3339 format). Only populated when pageId input is specified."
        },
        "draft": {
          "type": "boolean",
          "description": "Indicates if the page is in draft mode (read-only). Only populated when pageId input is specified."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the page was last updated (read-only, RFC3339 format). Only populated when pageId input is specified."
        },
        "pageId": {
          "type": "string",
          "description": "The specific page ID to retrieve (optional, 24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c4'). If specified, only this page's data will be returned. If omitted, all pages for the site will be returned in the 'pages' output."
        },
        "pages": {
          "type": "array",
          "items": {
            "$ref": "#/types/webflow:index:PageInfo"
          },
          "description": "List of all pages in the site (read-only). Only populated when pageId input is NOT specified. Each page includes all metadata fields: id, siteId, title, slug, createdOn, lastUpdated, archived, draft, and optional parentId/collectionId."
        },
        "parentId": {
          "type": "string",
          "description": "The ID of the parent page (read-only, optional). Only present for nested pages. Only populated when pageId input is specified."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "slug": {
          "type": "string",
          "description": "The URL slug for the page (read-only, e.g., 'about' for '/about'). Only populated when pageId input is specified."
        },
        "title": {
          "type": "string",
          "description": "The page title (read-only). This is the title shown in browser tabs and search results. Only populated when pageId input is specified."
        },
        "webflowPageId": {
          "type": "string",
          "description": "The Webflow page ID (read-only). Only populated when pageId input is specified."
        }
      },
      "required": [
        "siteId"
      ],
      "inputProperties": {
        "pageId": {
          "type": "string",
          "description": "The specific page ID to retrieve (optional, 24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c4'). If specified, only this page's data will be returned. If omitted, all pages for the site will be returned in the 'pages' output."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        }
      },
      "requiredInputs": [
        "siteId"
      ]
    },
    "webflow:index:Redirect": {
      "description": "Manages HTTP redirects for a Webflow site. This resource allows you to define redirect rules for old URLs to new locations, supporting both permanent (301) and temporary (302) redirects.",
      "properties": {
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the redirect was created (RFC3339 format). This is automatically set when the redirect is created and is read-only."
        },
        "destinationPath": {
          "type": "string",
          "description": "The URL path to redirect to (e.g., '/new-page', '/home'). Must start with '/' and contain only valid URL characters. This is the location where users will be redirected when they visit the source path."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "sourcePath": {
          "type": "string",
          "description": "The URL path to redirect from (e.g., '/old-page', '/blog/2023'). Must start with '/' and contain only valid URL characters (letters, numbers, hyphens, underscores, slashes, dots). Query strings and fragments are not allowed in the source path."
        },
        "statusCode": {
          "type": "integer",
          "description": "The HTTP status code for the redirect. Must be either 301 or 302. 301 = permanent redirect (use when a page has moved permanently; search engines update their index). 302 = temporary redirect (use for maintenance or temporary page moves)."
        }
      },
      "required": [
        "siteId",
        "sourcePath",
        "destinationPath",
        "statusCode"
      ],
      "inputProperties": {
        "destinationPath": {
          "type": "string",
          "description": "The URL path to redirect to (e.g., '/new-page', '/home'). Must start with '/' and contain only valid URL characters. This is the location where users will be redirected when they visit the source path."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "sourcePath": {
          "type": "string",
          "description": "The URL path to redirect from (e.g., '/old-page', '/blog/2023'). Must start with '/' and contain only valid URL characters (letters, numbers, hyphens, underscores, slashes, dots). Query strings and fragments are not allowed in the source path."
        },
        "statusCode": {
          "type": "integer",
          "description": "The HTTP status code for the redirect. Must be either 301 or 302. 301 = permanent redirect (use when a page has moved permanently; search engines update their index). 302 = temporary redirect (use for maintenance or temporary page moves)."
        }
      },
      "requiredInputs": [
        "siteId",
        "sourcePath",
        "destinationPath",
        "statusCode"
      ]
    },
    "webflow:index:RegisteredScript": {
      "description": "Manages custom code scripts in the Webflow script registry. This resource allows you to register and manage externally hosted scripts that can be deployed across your Webflow site with version control and integrity verification.",
      "properties": {
        "canCopy": {
          "type": "boolean",
          "description": "Indicates whether the script can be copied when the site is duplicated. Default: false. When true, the script will be included when creating a copy of the site."
        },
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the script was created (RFC3339 format). This is automatically set by Webflow when the script is created and is read-only."
        },
        "displayName": {
          "type": "string",
          "description": "The user-facing name for the script (1-50 alphanumeric characters). This name is used to identify the script in the Webflow interface. Only letters (A-Z, a-z) and numbers (0-9) are allowed. Example valid names: 'CmsSlider', 'AnalyticsScript', 'MyCustomScript123'."
        },
        "hostedLocation": {
          "type": "string",
          "description": "The URI for the externally hosted script (e.g., 'https://cdn.example.com/my-script.js'). Must be a valid HTTP or HTTPS URL. The script should be publicly accessible and properly configured for cross-origin requests."
        },
        "integrityHash": {
          "type": "string",
          "description": "The Sub-Resource Integrity (SRI) hash for the script. Format: 'sha384-<hash>', 'sha256-<hash>', or 'sha512-<hash>'. SRI hashes help ensure that the script hasn't been modified in transit. You can generate an SRI hash using https://www.srihash.org/"
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the script was last updated (RFC3339 format). This is automatically updated by Webflow when the script is modified and is read-only."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "version": {
          "type": "string",
          "description": "The Semantic Version (SemVer) string for the script (e.g., '1.0.0', '2.3.1'). This helps track different versions of your script. See https://semver.org/ for more information on semantic versioning."
        }
      },
      "required": [
        "siteId",
        "displayName",
        "hostedLocation",
        "integrityHash",
        "version"
      ],
      "inputProperties": {
        "canCopy": {
          "type": "boolean",
          "description": "Indicates whether the script can be copied when the site is duplicated. Default: false. When true, the script will be included when creating a copy of the site."
        },
        "displayName": {
          "type": "string",
          "description": "The user-facing name for the script (1-50 alphanumeric characters). This name is used to identify the script in the Webflow interface. Only letters (A-Z, a-z) and numbers (0-9) are allowed. Example valid names: 'CmsSlider', 'AnalyticsScript', 'MyCustomScript123'."
        },
        "hostedLocation": {
          "type": "string",
          "description": "The URI for the externally hosted script (e.g., 'https://cdn.example.com/my-script.js'). Must be a valid HTTP or HTTPS URL. The script should be publicly accessible and properly configured for cross-origin requests."
        },
        "integrityHash": {
          "type": "string",
          "description": "The Sub-Resource Integrity (SRI) hash for the script. Format: 'sha384-<hash>', 'sha256-<hash>', or 'sha512-<hash>'. SRI hashes help ensure that the script hasn't been modified in transit. You can generate an SRI hash using https://www.srihash.org/"
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "version": {
          "type": "string",
          "description": "The Semantic Version (SemVer) string for the script (e.g., '1.0.0', '2.3.1'). This helps track different versions of your script. See https://semver.org/ for more information on semantic versioning."
        }
      },
      "requiredInputs": [
        "siteId",
        "displayName",
        "hostedLocation",
        "integrityHash",
        "version"
      ]
    },
    "webflow:index:RobotsTxt": {
      "description": "Manages robots.txt configuration for a Webflow site. This resource allows you to define crawler access rules and sitemap references.",
      "properties": {
        "content": {
          "type": "string",
          "description": "The robots.txt content in traditional format. Supports User-agent, Allow, Disallow, and Sitemap directives."
        },
        "lastModified": {
          "type": "string",
          "description": "RFC3339 timestamp of the last modification."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3')."
        }
      },
      "required": [
        "siteId",
        "content",
        "lastModified"
      ],
      "inputProperties": {
        "content": {
          "type": "string",
          "description": "The robots.txt content in traditional format. Supports User-agent, Allow, Disallow, and Sitemap directives."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3')."
        }
      },
      "requiredInputs": [
        "siteId",
        "content"
      ]
    },
    "webflow:index:Site": {
      "description": "Manages Webflow sites programmatically. This resource allows you to create, configure, and manage Webflow sites through infrastructure code. Create, Read, Update, and Delete operations are fully supported for complete site lifecycle management.",
      "properties": {
        "customDomains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of custom domains attached to the site (read-only in this release). Custom domain management will be available in a future release."
        },
        "dataCollectionEnabled": {
          "type": "boolean",
          "description": "Indicates whether data collection is enabled for this site (read-only). Controlled by your Webflow workspace settings, not per-site."
        },
        "dataCollectionType": {
          "type": "string",
          "description": "The type of data collection enabled for the site (read-only). Possible values: 'always' (always collect), 'optOut' (collect by default), 'disabled' (no collection). Controlled by your Webflow workspace settings, not per-site."
        },
        "displayName": {
          "type": "string",
          "description": "The human-readable name of the site as shown in the Webflow dashboard. Required - must be a non-empty string. Examples: 'My Marketing Site', 'Company Blog', 'Product Landing Page'. This is the name users will see when managing the site."
        },
        "lastPublished": {
          "type": "string",
          "description": "The RFC3339 timestamp of the last time the site was published (read-only). Automatically set by Webflow when changes are published to production."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The RFC3339 timestamp of the last time the site configuration was updated (read-only). Automatically set by Webflow when any site changes occur."
        },
        "parentFolderId": {
          "type": "string",
          "description": "The folder ID where the site will be organized in the Webflow dashboard. Optional - the site will be placed at the workspace root if not specified. This is useful for organizing multiple sites into logical groups within your workspace."
        },
        "previewUrl": {
          "type": "string",
          "description": "The URL to a preview image of the site (read-only). Automatically generated by Webflow for site thumbnails in the dashboard."
        },
        "publish": {
          "type": "boolean",
          "description": "Automatically publish the site after creation or updates. When set to true, the provider will publish the site to production after successfully creating or updating it. Default: false (manual publishing required). Note: Site must have at least one published version before automatic publishing will work. If publishing fails, the entire operation will fail with an error (site may exist but Pulumi will report failure). Recommendation: Set to false for initial site creation, then enable after first manual publish."
        },
        "shortName": {
          "type": "string",
          "description": "The slugified version of the site name used in URLs and for file organization. Optional - if not provided, Webflow will automatically generate one from the displayName. Must use only lowercase letters (a-z), numbers (0-9), and hyphens (-). No spaces, underscores, or special characters allowed. Must start and end with a letter or number (no leading/trailing hyphens). Examples: 'my-site', 'company-blog-2024', 'product-landing-page'."
        },
        "templateName": {
          "type": "string",
          "description": "The template to use for site creation. Optional - if not specified, Webflow will create a blank site. **WARNING: This field is IMMUTABLE.** Once set, it cannot be changed. Changing this value will trigger a REPLACE operation, which will: (1) DELETE your existing site and ALL its content (pages, CMS items, assets, etc.), (2) CREATE a new site with the new template, (3) REPLACE all dependent resources (redirects, robots.txt, etc.). This is a DESTRUCTIVE operation that cannot be undone. Use any valid Webflow template identifier (e.g., 'mast-framework', 'blank'). Consider using resource protection (`protect: true`) to prevent accidental replacement."
        },
        "timeZone": {
          "type": "string",
          "description": "The IANA timezone identifier for the site. Optional - Webflow will use its default timezone if not specified. Use standard IANA timezone identifiers (e.g., 'America/New_York', 'Europe/London', 'Asia/Tokyo', 'UTC'). This timezone is used for scheduling, analytics, and other time-sensitive features."
        },
        "workspaceId": {
          "type": "string",
          "description": "The Webflow workspace ID where the site will be created. Required for site creation (Enterprise workspace required by Webflow API). Example: '5f0c8c9e1c9d440000e8d8c3'. You can find your workspace ID in the Webflow dashboard under Account Settings > Workspace."
        }
      },
      "required": [
        "workspaceId",
        "displayName"
      ],
      "inputProperties": {
        "displayName": {
          "type": "string",
          "description": "The human-readable name of the site as shown in the Webflow dashboard. Required - must be a non-empty string. Examples: 'My Marketing Site', 'Company Blog', 'Product Landing Page'. This is the name users will see when managing the site."
        },
        "parentFolderId": {
          "type": "string",
          "description": "The folder ID where the site will be organized in the Webflow dashboard. Optional - the site will be placed at the workspace root if not specified. This is useful for organizing multiple sites into logical groups within your workspace."
        },
        "publish": {
          "type": "boolean",
          "description": "Automatically publish the site after creation or updates. When set to true, the provider will publish the site to production after successfully creating or updating it. Default: false (manual publishing required). Note: Site must have at least one published version before automatic publishing will work. If publishing fails, the entire operation will fail with an error (site may exist but Pulumi will report failure). Recommendation: Set to false for initial site creation, then enable after first manual publish."
        },
        "shortName": {
          "type": "string",
          "description": "The slugified version of the site name used in URLs and for file organization. Optional - if not provided, Webflow will automatically generate one from the displayName. Must use only lowercase letters (a-z), numbers (0-9), and hyphens (-). No spaces, underscores, or special characters allowed. Must start and end with a letter or number (no leading/trailing hyphens). Examples: 'my-site', 'company-blog-2024', 'product-landing-page'."
        },
        "templateName": {
          "type": "string",
          "description": "The template to use for site creation. Optional - if not specified, Webflow will create a blank site. **WARNING: This field is IMMUTABLE.** Once set, it cannot be changed. Changing this value will trigger a REPLACE operation, which will: (1) DELETE your existing site and ALL its content (pages, CMS items, assets, etc.), (2) CREATE a new site with the new template, (3) REPLACE all dependent resources (redirects, robots.txt, etc.). This is a DESTRUCTIVE operation that cannot be undone. Use any valid Webflow template identifier (e.g., 'mast-framework', 'blank'). Consider using resource protection (`protect: true`) to prevent accidental replacement."
        },
        "timeZone": {
          "type": "string",
          "description": "The IANA timezone identifier for the site. Optional - Webflow will use its default timezone if not specified. Use standard IANA timezone identifiers (e.g., 'America/New_York', 'Europe/London', 'Asia/Tokyo', 'UTC'). This timezone is used for scheduling, analytics, and other time-sensitive features."
        },
        "workspaceId": {
          "type": "string",
          "description": "The Webflow workspace ID where the site will be created. Required for site creation (Enterprise workspace required by Webflow API). Example: '5f0c8c9e1c9d440000e8d8c3'. You can find your workspace ID in the Webflow dashboard under Account Settings > Workspace."
        }
      },
      "requiredInputs": [
        "workspaceId",
        "displayName"
      ]
    },
    "webflow:index:SiteCustomCode": {
      "description": "Manages custom JavaScript code applied to a Webflow site. This resource allows you to apply registered custom scripts to a site and control where they are placed (header or footer). Custom scripts must be registered to the site first via the RegisterScript resource.",
      "properties": {
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the site's custom code was first created (RFC3339 format). This is automatically set when custom code is first applied and is read-only."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the site's custom code was last updated (RFC3339 format). This is automatically set and is read-only."
        },
        "scripts": {
          "type": "array",
          "items": {
            "$ref": "#/types/webflow:index:CustomScriptArgs"
          },
          "description": "A list of custom scripts to apply to the site. Each script must be registered to the site first. To remove individual scripts, simply exclude them from this list on the next update. If you have multiple scripts your app manages, ensure they are always included in this list."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        }
      },
      "required": [
        "siteId",
        "scripts"
      ],
      "inputProperties": {
        "scripts": {
          "type": "array",
          "items": {
            "$ref": "#/types/webflow:index:CustomScriptArgs"
          },
          "description": "A list of custom scripts to apply to the site. Each script must be registered to the site first. To remove individual scripts, simply exclude them from this list on the next update. If you have multiple scripts your app manages, ensure they are always included in this list."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        }
      },
      "requiredInputs": [
        "siteId",
        "scripts"
      ]
    },
    "webflow:index:User": {
      "description": "Manages users for a Webflow site. This resource allows you to invite users to your site with specified access groups. Users will receive an invitation email and must accept it to access paid content. Note: The user's email cannot be changed after creation.",
      "properties": {
        "accessGroups": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional list of access group slugs to assign to the user. Access groups control what content the user can access. Groups are assigned as type 'admin' (assigned via API or designer). Example: ['premium-members', 'beta-testers']. Access group slugs can be found in the Webflow dashboard under Users > Access Groups."
        },
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the user was created (RFC3339 format). This is automatically set and is read-only."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user to invite. The user will receive an invitation email at this address. IMPORTANT: The email cannot be changed after the user is created. Changing the email will require replacing the resource (delete + recreate)."
        },
        "invitedOn": {
          "type": "string",
          "description": "The timestamp when the user was invited (RFC3339 format). This is automatically set and is read-only."
        },
        "isEmailVerified": {
          "type": "boolean",
          "description": "Indicates whether the user has verified their email address. This is read-only and set by Webflow when the user verifies their email."
        },
        "lastLogin": {
          "type": "string",
          "description": "The timestamp when the user last logged in (RFC3339 format). This is read-only."
        },
        "lastUpdated": {
          "type": "string",
          "description": "The timestamp when the user was last updated (RFC3339 format). This is read-only."
        },
        "name": {
          "type": "string",
          "description": "Optional display name for the user. This will be shown in the Webflow dashboard and can be used in site personalization."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "status": {
          "type": "string",
          "description": "The status of the user. Possible values: 'invited' (invitation sent but not accepted), 'verified' (email verified), 'unverified' (registered but email not verified). This is read-only."
        },
        "userId": {
          "type": "string",
          "description": "The Webflow-assigned unique identifier for this user. This is automatically assigned when the user is created and is read-only."
        }
      },
      "required": [
        "siteId",
        "email"
      ],
      "inputProperties": {
        "accessGroups": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional list of access group slugs to assign to the user. Access groups control what content the user can access. Groups are assigned as type 'admin' (assigned via API or designer). Example: ['premium-members', 'beta-testers']. Access group slugs can be found in the Webflow dashboard under Users > Access Groups."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user to invite. The user will receive an invitation email at this address. IMPORTANT: The email cannot be changed after the user is created. Changing the email will require replacing the resource (delete + recreate)."
        },
        "name": {
          "type": "string",
          "description": "Optional display name for the user. This will be shown in the Webflow dashboard and can be used in site personalization."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        }
      },
      "requiredInputs": [
        "siteId",
        "email"
      ]
    },
    "webflow:index:Webhook": {
      "description": "Manages webhooks for a Webflow site. Webhooks allow you to receive real-time notifications when events occur in your Webflow site, such as form submissions, page updates, e-commerce orders, and more. Note: Webhooks cannot be updated in-place; any change to triggerType, url, or filter requires replacement.",
      "properties": {
        "createdOn": {
          "type": "string",
          "description": "The timestamp when the webhook was created (RFC3339 format). This is automatically set by Webflow when the webhook is created and is read-only."
        },
        "filter": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Optional filter for webhook events. The structure depends on the triggerType and allows you to receive only specific events. For example, for collection_item_created, you can filter by collection ID. Refer to Webflow API documentation for filter options for each trigger type."
        },
        "lastTriggered": {
          "type": "string",
          "description": "The timestamp when the webhook was last triggered (RFC3339 format). This is automatically updated by Webflow when the webhook fires and is read-only. Will be empty if the webhook has never been triggered."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "triggerType": {
          "type": "string",
          "description": "The Webflow event that triggers this webhook. Valid values: form_submission, site_publish, page_created, page_metadata_updated, page_deleted, ecomm_new_order, ecomm_order_changed, ecomm_inventory_changed, memberships_user_account_added, memberships_user_account_updated, memberships_user_account_deleted, collection_item_created, collection_item_changed, collection_item_deleted, collection_item_unpublished. Example: 'form_submission' to receive notifications when forms are submitted."
        },
        "url": {
          "type": "string",
          "description": "The HTTPS endpoint where Webflow will send webhook events (e.g., 'https://example.com/webhooks/webflow', 'https://api.example.com/events'). Must be a valid HTTPS URL. Webflow requires HTTPS for security. Your endpoint should accept POST requests with JSON payloads containing event data."
        }
      },
      "required": [
        "siteId",
        "triggerType",
        "url"
      ],
      "inputProperties": {
        "filter": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Optional filter for webhook events. The structure depends on the triggerType and allows you to receive only specific events. For example, for collection_item_created, you can filter by collection ID. Refer to Webflow API documentation for filter options for each trigger type."
        },
        "siteId": {
          "type": "string",
          "description": "The Webflow site ID (24-character lowercase hexadecimal string, e.g., '5f0c8c9e1c9d440000e8d8c3'). You can find your site ID in the Webflow dashboard under Site Settings. This field will be validated before making any API calls."
        },
        "triggerType": {
          "type": "string",
          "description": "The Webflow event that triggers this webhook. Valid values: form_submission, site_publish, page_created, page_metadata_updated, page_deleted, ecomm_new_order, ecomm_order_changed, ecomm_inventory_changed, memberships_user_account_added, memberships_user_account_updated, memberships_user_account_deleted, collection_item_created, collection_item_changed, collection_item_deleted, collection_item_unpublished. Example: 'form_submission' to receive notifications when forms are submitted."
        },
        "url": {
          "type": "string",
          "description": "The HTTPS endpoint where Webflow will send webhook events (e.g., 'https://example.com/webhooks/webflow', 'https://api.example.com/events'). Must be a valid HTTPS URL. Webflow requires HTTPS for security. Your endpoint should accept POST requests with JSON payloads containing event data."
        }
      },
      "requiredInputs": [
        "siteId",
        "triggerType",
        "url"
      ]
    }
  },
  "functions": {
    "webflow:index:getAuthorizedUser": {
      "description": "Retrieves information about the user who authorized the current Webflow API token. This is useful for auditing and understanding which user's credentials are being used. Requires the 'authorized_user:read' scope.",
      "inputs": {
        "type": "object"
      },
      "outputs": {
        "properties": {
          "email": {
            "description": "The email address of the authorized user.",
            "type": "string"
          },
          "firstName": {
            "description": "The first name of the authorized user.",
            "type": "string"
          },
          "lastName": {
            "description": "The last name of the authorized user.",
            "type": "string"
          },
          "userId": {
            "description": "The unique identifier for the authorized user.",
            "type": "string"
          }
        },
        "required": [
          "userId",
          "email",
          "firstName",
          "lastName"
        ],
        "type": "object"
      }
    },
    "webflow:index:getTokenInfo": {
      "description": "Retrieves information about the current Webflow API token, including authorization details, scopes, rate limits, and the authorized resources. This is useful for validating your API token configuration and understanding what resources it can access.",
      "inputs": {
        "type": "object"
      },
      "outputs": {
        "properties": {
          "application": {
            "$ref": "#/types/webflow:index:GetTokenInfoApplication",
            "description": "Application details for the token owner."
          },
          "authorization": {
            "$ref": "#/types/webflow:index:GetTokenInfoAuthorization",
            "description": "Authorization details for the API token, including scopes and authorized resources."
          }
        },
        "required": [
          "authorization",
          "application"
        ],
        "type": "object"
      }
    }
  }
}
