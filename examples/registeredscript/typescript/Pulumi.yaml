name: webflow-registeredscript-example
runtime: nodejs
description: Example of registering and managing custom JavaScript scripts with Pulumi and Webflow

config:
  siteId:
    description: The Webflow site ID
    secret: true
  environment:
    description: Deployment environment (development, staging, production)
    default: development
