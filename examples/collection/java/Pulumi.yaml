name: webflow-collection-example
runtime: java
description: Example of creating and managing Collection resources with Pulumi and Webflow

config:
  siteId:
    description: The Webflow site ID
    secret: true
  environment:
    description: Deployment environment (development, staging, production)
    default: development
