name: webflow-redirect-example-java
runtime: java
description: Example of creating and managing Redirect resources with Pulumi and Webflow

config:
  siteId:
    description: The Webflow site ID
    secret: true
  environment:
    description: Deployment environment (development, staging, production)
    default: development
