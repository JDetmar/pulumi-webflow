name: webflow-robotstxt-example
runtime: nodejs
description: Example of creating and managing RobotsTxt resources with Pulumi and Webflow

config:
  siteId:
    description: The Webflow site ID
    secret: true
  environment:
    description: Deployment environment (development, staging, production)
    default: development
