name: webflow-sitecustomcode-example
runtime: nodejs
description: Example of applying custom scripts site-wide with Pulumi and Webflow

config:
  siteId:
    description: The Webflow site ID
    secret: true
  environment:
    description: Deployment environment (development, staging, production)
    default: development
