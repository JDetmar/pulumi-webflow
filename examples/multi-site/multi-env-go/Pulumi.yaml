name: webflow-multi-site-multi-env
runtime: go
description: Multi-Site Management Example - Go (Multi-Environment)

config:
  webflow:apiToken:
    description: Webflow API token (from WEBFLOW_API_TOKEN environment variable)
    secret: true
